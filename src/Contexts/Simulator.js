import { createContext } from "react";

const SimulatorInputContenxt = createContext(null)
const SimulatorOutputContenxt = createContext(null)

export {
    SimulatorInputContenxt,
    SimulatorOutputContenxt
}